apiVersion: v1
kind: Service
metadata:
  name: playground
  namespace: dev
  labels:
    app: playground
spec:
  type: ClusterIP

  ports:
  - name: http
    port: 8888
    targetPort: http
    protocol: TCP

  selector:
    app: playground

  sessionAffinity: None
